# Copy this file to Pipeline/.env and fill in the key for the provider you use.
# Pipeline/.env is gitignored and must never be committed.

# Default provider. Free key from https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Only needed for --provider openai
OPENAI_API_KEY=
