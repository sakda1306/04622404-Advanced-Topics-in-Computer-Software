[pytest]
# db.py keeps one global async engine, so every async test/fixture must share
# one event loop; otherwise the 2nd+ DB test can't reuse the pooled connection
# and `require_db` silently skips it.
asyncio_mode = auto
asyncio_default_test_loop_scope = session
asyncio_default_fixture_loop_scope = session
testpaths = tests
