# เอกสารเตรียมสอบ: ปัญหาและการแก้ไขปัญหาของระบบ RAG
### กรณีศึกษา: Sexual Health Q&A RAG System (Thai)

---

## หมายเหตุก่อนอ่าน

เอกสารนี้สร้างจากการอ่านซอร์สโค้ดจริงสองส่วนที่อาจารย์เตรียมไว้ให้ศึกษา คือ

1. **`DL-04-RAG System Development I/04-RAG-Project`** — ระบบ RAG ฉบับสมบูรณ์ ที่มี pipeline ครบทุกขั้นตอน ตั้งแต่โหลดข้อมูล ไปจนถึงสร้างคำตอบและประเมินผล ใช้เป็น "ระบบ RAG ที่พัฒนาขึ้น" ในการวิเคราะห์นี้
2. **`DL-05-RAG System Development II`** — ชุดสาธิตปัญหา 9 หัวข้อ (`problem01`–`problem09`) ที่จำลองปัญหาแต่ละแบบแยกออกมาให้เห็นชัด ๆ โดยใช้ชุดข้อมูลเดียวกัน (`sex_q_a.txt`)

ทั้งสองโปรเจกต์ใช้ฐานความรู้เดียวกันคือ `sex_q_a.txt` (ตรวจสอบแล้วว่าไฟล์เหมือนกันทุกไบต์ — md5 ตรงกัน) ซึ่งมีทั้งหมด **391 คู่ถาม-ตอบ** แบ่งเป็น **6 หมวดหมู่** (สุขภาพทางเพศและการป้องกัน, สรีระและการตอบสนองทางเพศ, การเจริญพันธุ์และการวางแผนครอบครัว, ความยินยอมและความสัมพันธ์, ความหลากหลายทางเพศและสุขภาพ, กฎหมายและบริบทสังคมไทย) และมี **3 ระดับภาษา** (ทางการ / กันเอง / แสลง) ต่อคำถามคล้ายกัน

โจทย์บรรยาย (essay) ของอาจารย์ระบุให้อธิบายโดย "สอดคล้องกับการทำงานและ Source Code ของระบบ RAG ที่นักศึกษาพัฒนาขึ้นจริง" ดังนั้นส่วนที่ 1–4 ด้านล่างนี้ยึดกรอบนั้น คืออธิบายปัญหา/การแก้ไขโดยอิงกับ pipeline และซอร์สโค้ดจริงของโปรเจกต์ตัวอย่าง เพื่อให้นักศึกษานำไปปรับเป็นความเข้าใจของตัวเองต่อได้

แต่ข้อสอบจริงไม่ได้มีแค่โจทย์บรรยายอย่างเดียว — ยังมีคำถามปรนัยที่ถามแนวคิดพื้นฐานของ RAG/LLM แบบทั่วไป ไม่ผูกกับ "ระบบที่ตนเองพัฒนาขึ้น" เลย (เช่น ผลของจำนวน Token ต่อระบบ, จุดประสงค์ของการทำ Chunking) เอกสารนี้จึงมี **ส่วนที่ 5** เพิ่มเติมไว้ รวบรวมแนวคิดพื้นฐานเหล่านี้แยกต่างหาก สำหรับเตรียมสอบข้อปรนัยโดยเฉพาะ

นอกจากนี้ยังตรวจสอบ repository ส่วนตัวที่พัฒนาต่อจริง (`04622404-Advanced-Topics-in-Computer-Software`) ซึ่งนำโครง pipeline เดียวกันไปปรับใช้กับโดเมนใหม่ (ควิซความรู้ฟุตบอล) — นี่คือ "ระบบที่นักศึกษาพัฒนาขึ้นจริง" ตามคำในโจทย์บรรยายอย่างตรงตัวที่สุด จึงเพิ่ม **ส่วนที่ 6** วิเคราะห์บั๊ก/ปัญหาจริงที่พบในโค้ดชุดนี้โดยเฉพาะ ซึ่งเหมาะเอาไปใช้ตอบโจทย์บรรยายมากกว่าเนื้อหาในส่วนที่ 1–5 ที่อ้างจากโค้ดของอาจารย์

---

## 1. ภาพรวมสถาปัตยกรรมของระบบ

ระบบเป็น RAG แบบ Retrieval-Augmented Generation ที่ตอบคำถามด้านสุขภาพทางเพศเป็นภาษาไทย โครงสร้างแบ่งเป็นสองช่วงเวลา

**ช่วงเตรียมข้อมูล (Indexing time — `build_index.py`)**: โหลดไฟล์ `sex_q_a.txt` ด้วย `document_loader.py` → แบ่งเป็น chunk ด้วย `text_splitter.py` → แปลงเป็นเวกเตอร์ด้วย `embedding_model.py` (โมเดล `paraphrase-multilingual-MiniLM-L12-v2`) → สร้างดัชนี FAISS (`vector_store.py`) และดัชนี BM25 (`hybrid_retriever.py`) → บันทึก metadata ของดัชนีไว้ตรวจสอบภายหลัง (`index_meta.py`)

**ช่วงตอบคำถาม (Query time — `main.py` → `RAGPipeline`)**: คำถามผู้ใช้ผ่าน `query_transform.py` (ปรับคำแสลง/เขียนคำถามใหม่) → `hybrid_retriever.py` (ค้นแบบ dense + BM25 แล้วรวมผลด้วย RRF) → `rerankers.py` (ตัวเลือก จัดอันดับใหม่ด้วย cross-encoder) → `generator.py` (ให้ LLM เขียนคำตอบโดยอ้างอิงเฉพาะ context ที่ค้นได้) → `memory.py` (จำบทสนทนาไว้ใช้ในคำถามต่อเนื่อง)

ทุกองค์ประกอบเปิด/ปิดได้ผ่าน `config.py` ซึ่งเป็นจุดศูนย์กลางที่ควบคุมพฤติกรรมทั้งระบบ และมีระบบ evaluation แยกต่างหาก (`evaluation/`) สำหรับวัดคุณภาพทั้งขั้นค้นหาและขั้นสร้างคำตอบด้วยตัวเลข ไม่ใช่ความรู้สึก

จุดที่ควรเข้าใจให้แม่นคือ **แต่ละขั้นตอนเป็นจุดที่ RAG ล้มเหลวได้อิสระจากกัน** — retrieval ถูกต้องไม่ได้แปลว่า generation จะถูกต้องตาม และ dense embedding เก่งเรื่องความหมายไม่ได้แปลว่าจะเก่งเรื่องคำเฉพาะทาง นี่คือเหตุผลที่ระบบต้องมีกลไกป้องกันปัญหาซ้อนกันหลายชั้น ไม่ใช่พึ่งพาส่วนใดส่วนหนึ่งเพียงอย่างเดียว

---

## 2. วิเคราะห์ปัญหาแยกตามขั้นตอนการทำงาน

### 2.1 คุณภาพข้อมูลต้นทาง (Data Quality)

**ปัญหา**: ข้อมูลดิบมักมีรายการซ้ำ (duplicate) และสัญญาณรบกวน (noise) เช่น ช่องว่างเกิน สัญลักษณ์แปลกปลอมจากการ scrape หรือ OCR ซึ่ง `problem03_data_quality.py` จำลองไว้ชัดเจน โดยสร้างตัวอย่าง noisy จากคำถามจริง แล้วเปรียบเทียบก่อน/หลัง normalize

**สาเหตุ**: ถ้าไม่มีขั้นตอนทำความสะอาดข้อมูลก่อนสร้าง embedding ระบบจะฝัง (embed) ข้อความที่ต่างกันแค่รูปแบบผิวเผิน (whitespace, ตัวพิมพ์, สัญลักษณ์) เป็นเวกเตอร์คนละตัว ทำให้เปลืองพื้นที่ดัชนีและอาจทำให้คะแนนความคล้ายกระจายตัวผิดเพี้ยน

**วิธีตรวจสอบ**: ฟังก์ชัน `normalize()` ใน `problem03_data_quality.py` ทำ 3 อย่าง คือแปลงเป็นตัวพิมพ์เล็ก ตัดสัญลักษณ์รบกวน (`_@!-`) และยุบช่องว่างซ้ำ จากนั้นใช้ `dict.fromkeys()` เพื่อลบค่าซ้ำ เมื่อรันกับไฟล์ `sex_q_a.txt` จริงทั้งไฟล์ (391 ข้อ) จะได้ตัวเลขจำนวนคำถามที่ซ้ำกันหลัง normalize ออกมาเป็นหลักฐานเชิงประจักษ์

**การแก้ไข**: ทำ normalization + deduplication เป็นขั้นตอนบังคับก่อนเข้า `build_index.py` ข้อสังเกตคือ `document_loader.py` ในโปรเจกต์หลัก (DL-04) **ยังไม่มี** ขั้นตอนนี้จริง ๆ — มันแค่ตัดช่องว่างหัวท้ายบรรทัด (`strip()`) เท่านั้น ไม่ได้ dedup หรือ normalize สัญลักษณ์ นี่คือช่องว่างที่พบได้จริงระหว่างสิ่งที่ `problem03` สาธิตไว้ กับสิ่งที่ระบบจริงทำ ถ้านำไปใช้งานต่อควรดึงฟังก์ชัน `normalize()` มาเสียบเป็นขั้นตอนก่อน `save_json(records, ...)` ใน `build_index.py`

### 2.2 การโหลดและแปลงข้อมูล (Document Loading / Parsing)

**ปัญหา**: `document_loader.py` แปลงไฟล์ข้อความเป็น record ด้วยการอ่านทีละบรรทัดและตรวจ prefix ตายตัว (`[หมวด`, `Q:`, `A:`) วิธีนี้เปราะบาง (brittle parsing) — ถ้าบรรทัดใดผิดรูปแบบเพียงเล็กน้อย (เช่น ไม่มี `A:` ตามหลัง `Q:` ทันที หรือมีบรรทัดว่างแทรกกลางคู่ถาม-ตอบ) รายการนั้นจะถูก "ข้ามอย่างเงียบ ๆ" โดยไม่มีข้อความแจ้งเตือน เพราะโค้ดใช้ `elif` ไล่ทีละเงื่อนไข ไม่มี branch สำหรับ "รูปแบบไม่ตรงเงื่อนไขใดเลย"

**สาเหตุ**: การ parse ด้วย string prefix ทำงานได้เร็วและเข้าใจง่าย แต่ไม่มีการ validate ว่าจำนวน record ที่โหลดได้ตรงกับจำนวนบล็อกในไฟล์จริงหรือไม่ ต่างจาก `data_loader.py` ในฝั่ง DL-05 ที่ใช้วิธีแบ่งบล็อกด้วยบรรทัดว่าง (`raw.split("\n\n")`) แล้วค่อยตรวจ regex กับบรรทัดแรกของบล็อก ซึ่งเป็นแนวทางที่ทนทานกว่าเล็กน้อยเพราะยึดโครงสร้าง "1 บล็อก = 1 คู่ถามตอบ" เป็นหลัก

**วิธีตรวจสอบ**: ใน `build_index.py` มีการเช็คเบื้องต้นอยู่แล้วคือ `if not records: print("No Q&A pairs found...")` แต่นี่จับได้เฉพาะกรณี "โหลดไม่ได้เลยสักข้อ" ไม่ใช่กรณี "โหลดได้บางส่วนแต่ขาดไปหลายข้อ" การตรวจที่แม่นยำกว่าคือเทียบจำนวน `grep -c "^Q:"` ในไฟล์ต้นฉบับกับ `len(records)` ที่โหลดได้จริง (ในกรณีนี้ควรได้ 391 เท่ากันทั้งคู่)

**การแก้ไข**: เพิ่ม log จำนวนบรรทัดที่ข้าม หรือเปรียบเทียบ count ก่อน-หลัง เพื่อให้มั่นใจว่าไม่มีข้อมูลหายระหว่างขั้นตอนนี้ เพราะถ้าข้อมูลหายตั้งแต่ต้นทาง จะไม่มีทางแก้ไขที่ปลายทาง (retrieval หา chunk ที่ไม่เคยถูกโหลดเข้าไม่ได้อยู่แล้ว)

### 2.3 การแบ่ง Chunk (Chunking)

**ปัญหา**: `problem04_chunking.py` แสดงให้เห็นสองขั้วของปัญหา — chunk **ใหญ่เกินไป** (size=200 คำ) จะรวมหลายคู่ถาม-ตอบเข้าด้วยกัน ทำให้ embedding เจือจาง (diluted) ไม่โฟกัสประเด็นเดียว ส่วน chunk **เล็กเกินไป** (size=15 คำ) จะตัดกลางประโยคอธิบาย ทำให้ใจความขาดตอน

**สาเหตุ**: การแบ่ง chunk แบบตัดตามความยาวคงที่ (fixed-size splitting) ไม่สนใจขอบเขตความหมายของประโยค เป็นวิธีที่ง่ายแต่เสี่ยงตัดขาดใจความ

**วิธีตรวจสอบ**: เทียบผลลัพธ์การค้นหาระหว่าง chunk size ต่าง ๆ ด้วย `evaluation/eval_retrieval.py` (ดู nDCG/Recall เปลี่ยนไปแค่ไหนเมื่อปรับ `CHUNK_SIZE`)

**การแก้ไขในระบบจริง**: `text_splitter.py` ของโปรเจกต์หลักแก้ปัญหานี้ด้วยกลยุทธ์เฉพาะของ dataset — เพราะข้อมูลเป็น Q&A ที่คำตอบส่วนใหญ่สั้นกว่า `CHUNK_SIZE=400` ตัวอักษรอยู่แล้ว ฟังก์ชัน `split_text()` จะคืนค่าทั้งก้อนโดยไม่ตัดเลย ถ้า `len(text) <= chunk_size` (ดูเงื่อนไข `if len(text) <= chunk_size: return [text]`) หมายความว่า **คู่ถาม-ตอบส่วนใหญ่จะกลายเป็น 1 chunk ที่สมบูรณ์ในตัวเอง** หลีกเลี่ยงปัญหา "ตัดกลางคำตอบ" ได้เป็นส่วนใหญ่ มีเพียงคำตอบที่ยาวเกิน 400 ตัวอักษรเท่านั้นที่จะถูกแบ่งเป็นหลายส่วนโดยมี `CHUNK_OVERLAP=50` ตัวอักษรกันใจความขาดตอนตรงรอยต่อ นี่คือตัวอย่างที่ดีของการ "ออกแบบ chunking ให้เข้ากับธรรมชาติของข้อมูล" แทนที่จะใช้ค่าคงที่แบบเดียวกับทุก dataset

### 2.4 Embedding และปัญหา Vocabulary Mismatch

**ปัญหา**: `problem02_transformer.py` แสดงให้เห็นว่าการค้นหาด้วย Bag-of-Words (BoW) ธรรมดา (นับคำซ้ำ) ไม่สามารถจับคู่คำถามที่ความหมายเดียวกันแต่ใช้คำต่างกันได้ เช่น คำถามแบบทางการที่ใช้คำว่า "ถุงยางอนามัย" กับคำถามแบบแสลงที่ใช้คำว่า "ถุงยาง" อาจมี token ที่ตรงกันน้อยมากจนคะแนนความคล้ายต่ำเกินจริง (exact-token overlap ต่ำ) ทั้งที่เป็นเรื่องเดียวกัน และ BoW ยังไม่รักษาลำดับคำ (position) ทำให้ประโยคที่สลับคำกันได้ representation เดียวกัน (`bow(a) == bow(b)` แม้ความหมายอาจเปลี่ยน)

**สาเหตุ**: BoW/keyword matching มองแต่ละคำเป็นหน่วยอิสระ ไม่มีมิติของความหมายเชิงบริบท (semantic) หรือลำดับคำ (positional information)

**วิธีตรวจสอบ**: เปรียบเทียบ token ที่ตรงกันตรง ๆ ระหว่างคำถามคู่ทางการ-แสลงที่พูดเรื่องเดียวกัน ถ้า intersection ว่างหรือน้อยมาก แปลว่าระบบ keyword-only จะพลาดคู่นี้แน่นอน

**การแก้ไข**: ระบบจริงแก้ด้วย 2 ชั้นซ้อนกัน ชั้นแรกคือ **dense embedding** (`embedding_model.py` ใช้ `sentence-transformers` กับโมเดล multilingual ที่เข้าใจความหมายข้ามคำศัพท์ได้ในระดับหนึ่ง) ชั้นที่สองคือ **`normalize_query()`** ใน `query_transform.py` ซึ่งมี `SLANG_MAP` เป็น dictionary แปลคำแสลงเป็นศัพท์ทางการตรง ๆ ก่อนค้นหา (เช่น "จู๋" → "อวัยวะเพศชาย", "เอดส์" → "เอชไอวี") ฟังก์ชันนี้ทำงานเสมอไม่ว่าจะเปิด `USE_QUERY_TRANSFORM` หรือไม่ (เป็น Level 1 แบบไม่ใช้ AI จึงเร็วและฟรี) ข้อจำกัดที่ควรรู้คือ `SLANG_MAP` เป็นตารางคงที่ (fixed lookup table) ครอบคลุมเฉพาะคำที่ผู้พัฒนานึกถึงล่วงหน้าเท่านั้น ไม่ generalize ไปยังคำแสลงใหม่ ๆ ที่ไม่ได้ใส่ไว้ในตาราง — นี่คือข้อจำกัดที่แท้จริงของวิธีนี้ที่ dense embedding เพียงอย่างเดียวช่วยชดเชยได้บางส่วน

### 2.5 การสร้างและจัดเก็บดัชนี (Vector Store & Index Staleness)

**ปัญหา**: ดัชนี FAISS (`document.index`) และไฟล์ `chunk_store.json` ต้องมีลำดับ (index) ตรงกันเป๊ะ เพราะ FAISS คืนค่าตำแหน่ง (position) แล้วโค้ดใช้ตำแหน่งนั้นไป index เข้า `self.chunks[position]` ตรง ๆ ถ้าแก้ไขข้อมูลต้นฉบับ หรือเปลี่ยนค่า config ที่มีผลต่อการแบ่ง chunk (เช่น `CHUNK_SIZE`) แล้ว **ลืมรัน `build_index.py` ใหม่** ดัชนีเก่ากับข้อมูลใหม่จะไม่ตรงกัน ระบบจะค้นเจอ "ตำแหน่ง" ที่ถูก แต่เนื้อหาใน `chunk_store.json` ที่ตำแหน่งนั้นอาจไม่ใช่สิ่งที่ embedding เดิมชี้ถึงอีกต่อไป — เป็นปัญหาที่อันตรายเพราะไม่มี error ให้เห็น ระบบจะยังตอบคำถามได้ปกติ แต่ตอบผิด (silent corruption)

**สาเหตุ**: ไม่มีการผูก (bind) เวอร์ชันของดัชนีเข้ากับเวอร์ชันของข้อมูล/ค่าตั้งที่ใช้สร้างมันโดยอัตโนมัติ

**วิธีตรวจสอบ**: นี่คือจุดที่ `index_meta.py` เข้ามาแก้โดยเฉพาะ — ตอนสร้างดัชนีเสร็จ `index_meta.save()` จะบันทึกขนาดไฟล์+เวลาแก้ไขล่าสุดของ `sex_q_a.txt` และค่าที่อยู่ใน `TRACKED_SETTINGS = ["CHUNK_SIZE", "CHUNK_OVERLAP", "EMBEDDING_MODEL_NAME"]` ลงไฟล์ `index_meta.json` ทุกครั้งที่ `main.py` เริ่มทำงาน จะเรียก `index_meta.warn_if_stale()` ซึ่งเทียบค่าปัจจุบันกับค่าที่บันทึกไว้ ถ้าไม่ตรงกันจะพิมพ์คำเตือนทันที ("! index ไม่ตรงกับ dataset ปัจจุบัน") พร้อมระบุว่าอะไรเปลี่ยนไป

**การแก้ไข**: รัน `python build_index.py` ใหม่ทุกครั้งหลังแก้ dataset หรือค่าตั้งที่เกี่ยวกับการแบ่ง chunk/embedding กลไก `warn_if_stale()` คือตัวอย่างของ "defensive programming" ที่ดี — มันไม่ได้ป้องกันปัญหาไม่ให้เกิด แต่ทำให้ปัญหาที่มองไม่เห็น (silent) กลายเป็นปัญหาที่มองเห็นได้ (visible) ตั้งแต่ก่อนเริ่มใช้งาน

### 2.6 การค้นคืนข้อมูล: Dense vs BM25 vs Hybrid

**ปัญหา**: Dense retrieval อย่างเดียว (`retriever.py`) มีจุดอ่อนกับคำเฉพาะทาง คำย่อภาษาอังกฤษ หรือคำที่ต้องการ exact match เช่น "PrEP" กับ "PEP" ซึ่งสะกดใกล้กันมากแต่ความหมายต่างกันโดยสิ้นเชิง โมเดล embedding อาจให้เวกเตอร์ที่ใกล้กันเกินไปจนแยกไม่ออก ในทางกลับกัน BM25 (keyword search) เก่งเรื่อง exact/partial match แต่ไม่เข้าใจความหมายเชิงบริบทเลย

**สาเหตุเฉพาะของภาษาไทย**: BM25 ต้องมีการตัดคำ (tokenize) ก่อน แต่ภาษาไทยไม่มีช่องว่างระหว่างคำ ฟังก์ชัน `tokenize()` ใน `hybrid_retriever.py` จึงต้องพึ่ง `pythainlp.tokenize.word_tokenize` (engine `newmm`) ถ้าไลบรารีนี้ไม่ถูกติดตั้ง โค้ดจะ `except ImportError` แล้ว fallback ไปใช้วิธีตัดทุก 3 ตัวอักษรแบบเหลื่อมกัน (`text[i:i+3]`) ซึ่งตามคอมเมนต์ในซอร์สเองระบุตรง ๆ ว่า "ใช้ได้แต่แย่กว่าชัดเจน" — คุณภาพของ BM25 จึงขึ้นอยู่กับว่าเครื่องที่รันมี `pythainlp` ติดตั้งอยู่จริงหรือไม่ ถ้าไม่มี ผลการค้นหาด้วยคำไทยจะแย่ลงอย่างเงียบ ๆ โดยไม่มี error ใด ๆ แจ้งเตือน

**วิธีตรวจสอบ**: `evaluation/eval_retrieval.py` เปรียบเทียบ 3–4 การตั้งค่า (`dense_only`, `bm25_only`, `hybrid`, และ `hybrid+rerank` ถ้าเปิด) บน query 4 รูปแบบ (`verbatim`, `slang`, `partial`, `natural`) คอมเมนต์ในโค้ดระบุแนวทางตีความผลไว้ชัดเจนว่า hybrid ควรชนะ dense-only มากที่สุดในคำถามแบบ `partial` และคำย่อภาษาอังกฤษ ถ้าไม่เป็นแบบนั้นแปลว่ามีบางอย่างผิดปกติ (เช่น BM25 index ใช้ fallback tokenizer)

**การแก้ไข**: เปิด `USE_HYBRID = True` เพื่อรวมจุดแข็งทั้งสองวิธีผ่าน RRF (ดูหัวข้อถัดไป) และตรวจสอบให้แน่ใจว่า `pythainlp` ถูกติดตั้งจริงในสภาพแวดล้อมที่ deploy (ดูหัวข้อ 3 เรื่อง dependency ที่ตกหล่นเพิ่มเติม)

### 2.7 การรวมผลลัพธ์ด้วย Reciprocal Rank Fusion (RRF)

**ปัญหา**: เมื่อค้นหลายวิธี (dense + BM25) และหลาย query พร้อมกัน (จาก multi-query transform) จะได้ผลลัพธ์เป็นหลาย ranked list ที่ต้องรวมเป็นอันดับเดียว ถ้ารวมผิดวิธี (เช่น บวกคะแนนดิบตรง ๆ) จะมีปัญหาเรื่อง scale ไม่เท่ากันระหว่าง cosine similarity ของ dense กับคะแนน BM25 ที่ไม่มีขอบเขตตายตัว

**สาเหตุ**: คะแนนจากวิธีค้นหาต่างชนิดกันเทียบกันตรง ๆ ไม่ได้ (score จาก dense อยู่ในช่วง [-1,1] ส่วน BM25 score ไม่มีขอบบน)

**วิธีตรวจสอบ**: ทดลองปรับค่าคงที่ `RRF_K` แล้วดู nDCG/MRR เปลี่ยนแปลงผ่าน `eval_retrieval.py`

**การแก้ไข**: ฟังก์ชัน `reciprocal_rank_fusion()` ใน `hybrid_retriever.py` ใช้สูตร RRF ที่ไม่สนใจคะแนนดิบเลย สนใจแค่ **อันดับ (rank)** ของแต่ละ chunk ในแต่ละ ranked list โดยให้คะแนน `1 / (RRF_K + rank)` แล้วรวมคะแนนของ chunk เดียวกันที่ถูกพบจากหลายวิธี/หลาย query เข้าด้วยกัน วิธีนี้แก้ปัญหาเรื่อง scale ไม่ตรงกันได้เพราะไม่ต้องเทียบคะแนนดิบข้ามระบบเลย ค่า `RRF_K = 60` (ค่ามาตรฐานจากงานวิจัยต้นฉบับของ RRF) ทำหน้าที่ถ่วงน้ำหนัก — ถ้า `RRF_K` เล็ก อันดับต้น ๆ จะได้น้ำหนักเหนือกว่าอันดับท้ายมาก ถ้า `RRF_K` ใหญ่ ความแตกต่างระหว่างอันดับจะถูกทำให้แบนราบลง (คะแนนใกล้เคียงกันมากขึ้นในทุกอันดับ)

### 2.8 การปรับคำถามก่อนค้นหา (Query Transformation)

**ปัญหา**: คำถามจากผู้ใช้จริงมักสั้น กำกวม หรือพึ่งพาบริบทของคำถามก่อนหน้า (follow-up) เช่น "แล้วผลข้างเคียงล่ะ" ซึ่งค้นหาตรง ๆ ไม่ได้เลยถ้าไม่รู้ว่า "แล้ว...ล่ะ" หมายถึงอะไร

**สาเหตุ**: ฐานความรู้ใช้ศัพท์ทางการ แต่ผู้ใช้พิมพ์ภาษาพูด/แสลง/คำถามไม่สมบูรณ์ในตัวเอง

**วิธีตรวจสอบและแก้ไข**: `query_transform.py` วางกลยุทธ์เป็น 2 ระดับ **Level 1 (`normalize_query`)** ทำงานเสมอ ไม่ใช้ AI แทนคำแสลงด้วย dictionary และตัดคำลงท้าย ("ครับ", "ค่ะ" ฯลฯ) ที่ไม่ช่วยการค้นหาออก **Level 2 (`transform`)** ใช้ LLM เมื่อ `USE_QUERY_TRANSFORM = True` มี 3 โหมดให้เลือกผ่าน `QUERY_TRANSFORM_MODE` คือ `rewrite` (เขียนคำถามใหม่ให้สมบูรณ์ในตัวเอง โดยอาศัยบทสนทนาก่อนหน้า), `multi_query` (สร้างคำถามความหมายเดียวกันหลายเวอร์ชันแล้วค้นทุกเวอร์ชันพร้อมกัน), และ `hyde` (ให้ LLM แต่งคำตอบสมมติขึ้นมาก่อน แล้วใช้คำตอบนั้นเป็นคำค้น เพราะข้อความแบบคำตอบมักใกล้เคียงเอกสารจริงมากกว่าคำถาม)

**ความเสี่ยงที่ต้องระวัง**: การเรียก LLM เพิ่มทุกครั้งก่อนค้นหา (ตามคอมเมนต์ใน config "เสีย LLM เพิ่ม 1 ครั้งต่อคำถาม") ทำให้ latency เพิ่มขึ้นและมีค่าใช้จ่ายเพิ่ม และในโหมด HyDE มีความเสี่ยงว่า LLM อาจ "แต่งคำตอบสมมติ" ที่หลุดประเด็นหรือผิดข้อเท็จจริง (เพราะตามคอมเมนต์ในโค้ดเองบอกว่า "ไม่ต้องกังวลว่าข้อเท็จจริงถูกไหม") ซึ่งถ้าหลุดมากอาจพาการค้นหาออกนอกทาง โค้ดจึงกันความเสี่ยงนี้ไว้บางส่วนด้วยการเก็บคำถามเดิม (normalize แล้ว) ไว้เป็นหนึ่งใน query ที่ใช้ค้นเสมอ (`return [normalize_query(query), fake_answer]`) และทั้งหมดถูกครอบด้วย `try/except` — ถ้า LLM เรียกไม่สำเร็จ ระบบจะ fallback กลับไปใช้คำถามเดิมที่ normalize แล้วแทน ไม่ทำให้ระบบล่ม (graceful degradation)

### 2.9 การจัดอันดับใหม่ (Re-ranking)

**ปัญหา**: `problem06_reranking.py` แสดงว่า first-stage retrieval (ที่ให้น้ำหนักคำทั่วไปเท่า ๆ กัน) อาจจัดอันดับเอกสารที่ตรงประเด็นที่สุดไว้ต่ำเกินไป ตัวอย่างในโค้ด: คำทั่วไปอย่าง "เอชไอวี" และ "ตรวจ" ทำให้เอกสารหลายชิ้นได้คะแนนใกล้เคียงกันหมด แต่เอกสารที่ตอบตรงคำถามจริง ๆ (มีคำเฉพาะอย่าง "ระยะฟักตัว"/"window period") กลับไม่ได้อยู่อันดับต้น เพราะ first-stage ยังไม่ได้ให้น้ำหนักพิเศษกับคำเฉพาะเจาะจงเหล่านี้

**สาเหตุ**: bi-encoder (dense embedding แยกเข้ารหัส query กับ document อิสระจากกัน) และ BM25 วัดความคล้ายแบบหยาบ (coarse) เร็วแต่ไม่ละเอียด

**วิธีตรวจสอบ**: เปรียบเทียบอันดับก่อน/หลัง rerank เหมือนใน `problem06` หรือดูค่า MRR/nDCG ใน `eval_retrieval.py` ระหว่างเปิด/ปิด `USE_RERANK`

**การแก้ไข**: `rerankers.py` ใช้ **cross-encoder** (`BAAI/bge-reranker-v2-m3`) ซึ่งป้อน query กับ document เข้าโมเดลพร้อมกันเป็นคู่ (`pairs = [(query, chunk["text"]) for chunk in chunks]`) ทำให้โมเดลเห็นปฏิสัมพันธ์ระหว่างสองข้อความโดยตรง แม่นยำกว่ามากแต่ **ช้ากว่ามาก** เพราะต้องรันโมเดลแยกทีละคู่ ไม่ได้ index ไว้ล่วงหน้าเหมือน FAISS จึงต้องทำงานเป็น 2 stage เสมอ: ค้นกว้าง ๆ ด้วย `CANDIDATE_K=20` ก่อน แล้วค่อยให้ cross-encoder จัดอันดับใหม่เหลือ `TOP_K=3` ชิ้นสุดท้าย ระบบยังมีการป้องกันความล้มเหลว: ฟังก์ชัน `get_reranker()` ครอบการโหลดโมเดลด้วย `try/except` — ถ้าโหลดไม่สำเร็จ (เน็ตหลุด/แรมไม่พอ) จะคืนค่า `None` แทนที่จะทำให้โปรแกรมพัง ระบบจะยังทำงานต่อได้โดยแค่ข้ามขั้น rerank ไป (fail-open pattern) นี่คือเหตุผลที่ `USE_RERANK = False` เป็นค่าเริ่มต้น เพราะโมเดลมีขนาดใหญ่และช้ากว่าขั้นตอนอื่นอย่างมีนัยสำคัญ

### 2.10 การสร้างคำตอบ: Hallucination และ Faithfulness

นี่คือสองปัญหาที่สำคัญที่สุดในโดเมนสุขภาพ เพราะคำตอบผิดอาจเป็นอันตรายต่อผู้ใช้จริง

**ปัญหา A — Hallucination**: `problem01_hallucination.py` จำลองว่าถ้าไม่มีการป้องกัน โมเดลอาจ "แต่งคำตอบขึ้นมาเอง" แม้ retrieval จะหาอะไรไม่เจอเลย ตัวอย่างในโค้ด (`bad_generate`) คือกรณีคำถามที่อยู่นอกขอบเขตความรู้โดยสิ้นเชิง (เช่น ถามราคาตั๋วเครื่องบิน) แต่โมเดลจอมปลอมยังคงตอบข้อมูลสุขภาพที่ไม่มีอยู่จริงในฐานความรู้ออกมา

**การแก้ไขในระบบจริง — ป้องกันสองชั้น**: ชั้นแรกเป็นระดับ **prompt** — `SYSTEM_PROMPT` ใน `prompt_templates.py` มีกฎข้อ 2 ระบุตรง ๆ ว่า "ถ้าข้อมูลไม่พอ ให้ตอบว่า '{no_context}' ห้ามเดา" และกฎข้อ 1 บังคับว่า "ใช้เฉพาะข้อมูลใน 'ข้อมูลอ้างอิง' ห้ามเพิ่มความรู้จากภายนอก" ชั้นที่สองเป็นระดับ **โค้ด** ซึ่งแข็งแรงกว่าเพราะไม่ต้องพึ่งว่า LLM จะเชื่อฟัง prompt หรือไม่ — ฟังก์ชัน `Generator.generate()` ใน `generator.py` เช็คตั้งแต่ต้นว่า `if not chunks: return {"answer": config.NO_CONTEXT_MESSAGE, ...}` คือถ้า retrieval หาอะไรไม่เจอเลย ระบบจะไม่เรียก LLM เลยด้วยซ้ำ ตัดปัญหา hallucination จากจุดตั้งต้น

**ปัญหา B — Faithfulness (ค้นถูกแต่ตอบผิด)**: `problem07_generation.py` สาธิตกรณีที่อันตรายกว่า คือ retrieval หาเอกสารที่ถูกต้องเจอ แต่ขั้นตอนสร้างคำตอบ (generation) กลับบิดเบือนรายละเอียดสำคัญ ตัวอย่างในโค้ดคือข้อมูลเรื่อง PEP ที่ต้องเริ่มยาภายใน "72 ชั่วโมง" ถูกเปลี่ยนเป็น "7 วัน" อย่างไม่ตั้งใจ (`bad_generator` ใช้ `.replace()` จำลองความผิดพลาดแบบนี้) ซึ่งในโดเมนสุขภาพ ตัวเลขแบบนี้ผิดแม้แต่นิดเดียวก็เป็นอันตรายจริง

**สาเหตุ**: LLM มีธรรมชาติชอบ paraphrase/สรุปความ ซึ่งบางครั้งเปลี่ยนตัวเลขหรือเงื่อนไขสำคัญไปโดยไม่รู้ตัว โดยเฉพาะถ้า temperature สูงหรือ prompt ไม่บังคับให้ตอบตรงตามต้นฉบับ

**วิธีตรวจสอบ**: `evaluation/eval_generation.py` มีตัวชี้วัด `faithfulness` ที่คำนวณจาก `word_overlap(answer, context)` คือสัดส่วนคำในคำตอบที่ปรากฏอยู่ใน context จริงด้วย ถ้าคำตอบ "เรียบเรียง/คัดลอก" มาจากเอกสารจริง ค่านี้จะสูง ถ้า "แต่งขึ้นเอง" ค่าจะต่ำ สคริปต์ยังพิมพ์รายงาน "คำตอบที่ยึดตามเอกสารน้อยที่สุด" ออกมาให้ตรวจสอบด้วยตาเป็นลำดับความสำคัญ

**ข้อจำกัดของตัวชี้วัดนี้ที่ควรรู้**: `word_overlap` เป็นการวัดแบบ **lexical overlap** (นับคำที่ตรงกันตรง ๆ) ไม่ใช่การวัดความหมายเชิงลึก (semantic) ดังนั้นคำตอบที่ถูกต้องแต่ใช้คำพูดคนละแบบกับเอกสารต้นฉบับ (เช่น สรุปความหมายถูกแต่เปลี่ยนคำ) อาจถูกวัดว่า "faithfulness ต่ำ" ทั้งที่จริง ๆ ไม่ได้บิดเบือนอะไรเลย นี่คือตัวอย่างว่า "ปัญหาของ RAG" ไม่ได้จำกัดอยู่แค่ใน pipeline การตอบคำถาม แต่ยังเกิดขึ้นได้ใน **ขั้นตอนการประเมินผลเอง** ด้วย

**การแก้ไขเพิ่มเติม**: ตั้ง `LLM_TEMPERATURE = 0.2` ให้ต่ำ (ลดความสุ่ม/ความคิดสร้างสรรค์ของคำตอบ) บังคับให้มีการอ้างอิงเลข `[n]` ท้ายประโยค (วัดผลผ่าน `has_citation`) และถ้าการเรียก LLM ล้มเหลวทั้งหมด (`except Exception`) โค้ดจะ fallback ไปใช้ `chunks[0]["answer"]` คือคำตอบดิบจากเอกสารโดยตรง ไม่ผ่านการ generate เลย ซึ่งรับประกันว่าอย่างน้อยคำตอบจะไม่ถูกบิดเบือนจาก LLM ในกรณีฉุกเฉินนี้

### 2.11 หน่วยความจำบทสนทนา (Conversation Memory)

**ปัญหา**: ถ้าเก็บประวัติสนทนาไม่จำกัด prompt จะยาวขึ้นเรื่อย ๆ ทำให้ค่าใช้จ่ายและเวลาตอบเพิ่มขึ้น แต่ถ้าตัดประวัติทิ้งง่ายเกินไป ระบบอาจ "ลืม" หัวข้อตั้งต้นของบทสนทนาไปเลย

**สาเหตุ**: ต้องหาจุดสมดุลระหว่างขนาด context กับความสามารถในการจำบริบทระยะยาว

**การแก้ไข**: `memory.py` จำกัดด้วย `config.MEMORY_MAX_TURNS` (ค่าเริ่มต้น 6 รอบ = 12 ข้อความ นับผู้ใช้+ผู้ช่วยเป็น 1 รอบ) ผ่านฟังก์ชัน `forget_old_messages()` ที่ตัดข้อความเก่าสุดทิ้งแบบ FIFO เมื่อเกินโควตา จุดที่ออกแบบมาดีคือก่อนตัดทิ้ง โค้ดจะเก็บ **หัวข้อแรก** ที่คุย (`self.first_topic`) ไว้ต่างหาก แล้วแนบกลับเข้าไปใน context เสมอ (`[หัวข้อที่คุยก่อนหน้า: ...]`) เพื่อไม่ให้บทสนทนายาว ๆ หลุดประเด็นตั้งต้นไปเลยทั้งที่ตัดข้อความเก่าทิ้งไปแล้ว

**ข้อจำกัดที่ควรรู้**: ฟังก์ชัน `is_followup()` ที่ใช้ตัดสินว่าคำถามนี้ต้องพึ่งบริบทก่อนหน้าหรือไม่ (เพื่อตัดสินใจว่าจะส่งประวัติเข้า query transformer หรือไม่) ใช้ heuristic ง่าย ๆ คือ ความยาวข้อความ < 30 ตัวอักษร **และ** ขึ้นต้นด้วยคำในลิสต์ `("แล้ว", "มัน", "อันนั้น", "อันนี้", "ต่อ", "อีก", "ทำไม", "ล่ะ")` วิธีนี้จะพลาด (false negative) กับคำถามต่อเนื่องที่เขียนยาวและเป็นประโยคสมบูรณ์ (เช่น "ผลข้างเคียงของมันเป็นยังไงบ้างครับ" ที่ยาวกว่า 30 ตัวอักษรและไม่ได้ขึ้นต้นด้วยคำในลิสต์) ซึ่งจะทำให้ระบบไม่ส่งบริบทเข้าไปช่วยปรับคำถามทั้งที่ควรจะช่วย

### 2.12 การประเมินผล (Evaluation)

**ปัญหาที่ evaluation ต้องแก้**: ถ้าไม่มีตัวเลขวัดผล จะไม่มีทางรู้เลยว่าการเปิด hybrid หรือ rerank ช่วยจริงหรือไม่ หรือช่วยแค่ไหน

**วิธีแก้**: `evaluation/build_golden_set.py` สร้างชุดทดสอบอัตโนมัติจาก `chunk_store.json` เอง — เพราะทุก chunk มาจากคู่ถาม-ตอบจริงอยู่แล้ว จึงรู้ "คำตอบที่ถูกต้อง" (ground truth) โดยไม่ต้องมานั่ง label เอง จากนั้นแปลงคำถามต้นฉบับให้เป็น 4 รูปแบบ (`verbatim` คำถามต้นฉบับเป๊ะ ๆ, `slang` แทนศัพท์ทางการด้วยคำแสลง, `partial` ตัด stopword เหลือแต่คำสำคัญ, `natural` เติมคำนำ/คำลงท้ายแบบภาษาพูด) ช่องว่างของคะแนนระหว่าง `verbatim` กับ `natural` คือตัวชี้วัดว่าระบบรับมือกับคำถามผู้ใช้จริงได้ดีแค่ไหน ไม่ใช่แค่ตอบเก่งเฉพาะตอน "ถามตรงคำเป๊ะ"

`evaluation/metrics.py` คำนวณ Hit@k, Recall@k, Precision@k, MRR, nDCG@k โดยเขียนเองทั้งหมด (ไม่พึ่งไลบรารีสำเร็จรูป) เพื่อความโปร่งใส และ `metrics.py` เองมีชุดทดสอบ (`assert`) คำนวณด้วยมือเทียบไว้ท้ายไฟล์ เพื่อยืนยันว่าสูตรถูกต้องก่อนใช้งานจริง เป็นแนวทางที่ดีเวลาต้องเขียนตัวชี้วัดใหม่เอง

---

## 3. ปัญหาจริงที่ตรวจพบจากการอ่านซอร์สโค้ด (ไม่ใช่การจำลอง)

ข้อสอบเปิดช่องให้พูดถึง "ปัญหาที่เกิดขึ้นจริงและแก้ไขแล้ว" ต่อไปนี้คือปัญหาที่ตรวจพบจริงจากการอ่านโค้ดของโปรเจกต์ (ไม่ใช่สถานการณ์สมมติ) ซึ่งเป็นตัวอย่างที่ดีมากสำหรับตอบคำถามข้อนี้เพราะมีหลักฐานในซอร์สโค้ดชัดเจน

### 3.1 `requirements.txt` ขาด dependency ที่โค้ดใช้จริง

ตรวจสอบ `import` ทั้งหมดในโปรเจกต์เทียบกับ `requirements.txt` พบว่าไฟล์ระบุไว้เพียง `numpy`, `sentence-transformers`, `faiss-cpu`, `tqdm` แต่โค้ดจริงยังเรียกใช้ `openai` (ใน `src/generator.py`), `rank_bm25` (ใน `src/hybrid_retriever.py`), และ `pythainlp` (ใน `src/hybrid_retriever.py`) เพิ่มเติมด้วย

**ผลกระทบ**: `rank_bm25` และ `openai` ถูก import แบบ top-level (บนสุดของไฟล์) ไม่ได้ครอบด้วย `try/except` ดังนั้นถ้า clone โปรเจกต์มาใหม่แล้วติดตั้งแค่ตาม `requirements.txt` โปรแกรมจะ **ล้มทันทีด้วย `ModuleNotFoundError`** ตั้งแต่ก่อนรันโค้ดบรรทัดแรกเสียอีก (ส่วน `pythainlp` มี fallback ให้อยู่แล้วตามที่อธิบายในข้อ 2.6 จึงไม่ทำให้โปรแกรมพัง แต่คุณภาพ BM25 จะแย่ลง)

**การแก้ไข**: เพิ่ม `openai`, `rank_bm25`, `pythainlp` ลงใน `requirements.txt` ให้ครบ นี่คือปัญหาประเภท "environment/dependency management" ซึ่งเป็นปัญหาที่พบได้บ่อยมากเวลาโปรเจกต์ RAG ถูกส่งต่อ/deploy ไปเครื่องอื่น และเป็นเหตุผลว่าทำไมควรทดสอบการติดตั้งจาก `requirements.txt` ในสภาพแวดล้อมที่สะอาด (clean environment) ก่อนส่งมอบงานเสมอ

### 3.2 โหมด `USE_LLM = False` ใช้งานไม่ได้ตามที่ตั้งใจไว้จริง (bug ที่ยืนยันได้จากโค้ด)

ใน `config.py` คอมเมนต์ของ `USE_LLM` เขียนไว้ว่า `"False = แสดงข้อความที่ค้นได้ดิบ ๆ ไม่เรียก LLM เลย"` และ `generator.py` มีคลาส `NoLLM` ไว้รองรับกรณีนี้โดยเฉพาะ โดยพยายามดึงข้อความมาจาก prompt string ที่ประกอบไว้แล้วด้วยการหาคำว่า `"reference data :"` และ `"Q of user"` เป็นตัวแบ่ง:

```python
parts = user_message.split("reference data :")
if len(parts) < 2:
    return config.NO_CONTEXT_MESSAGE
context = parts[1].split("Q of user")[0].strip()
```

แต่เมื่อตรวจ `prompt_templates.py` ที่เป็นคนสร้าง prompt จริง กลับพบว่า `USER_PROMPT` ถูกเขียนเป็น **ภาษาไทยทั้งหมด** และไม่มีคำว่า `"reference data :"` หรือ `"Q of user"` อยู่เลยสักที่เดียว (ใช้คำว่า `"ข้อมูลอ้างอิง:"` และ `"คำถามของผู้ใช้:"` แทน)

**ผลกระทบที่ตรวจสอบได้**: เพราะ `"reference data :"` ไม่เคยปรากฏใน prompt ภาษาไทยจริง เงื่อนไข `if len(parts) < 2` จะเป็นจริงเสมอ ทำให้ `NoLLM.chat()` **คืนค่า `config.NO_CONTEXT_MESSAGE` ("ขออภัย ไม่พบข้อมูลที่เกี่ยวข้อง") ทุกครั้งไม่ว่าการค้นหาจะเจอ context ที่ถูกต้องหรือไม่ก็ตาม** เมื่อตั้ง `USE_LLM = False` ซึ่งขัดกับสิ่งที่คอมเมนต์บอกไว้โดยสิ้นเชิง (ควรจะโชว์คำตอบดิบที่ค้นเจอ ไม่ใช่บอกว่าไม่พบข้อมูลเสมอ)

**สาเหตุ**: นี่คือร่องรอยของโค้ดที่แต่เดิมเขียน prompt เป็นภาษาอังกฤษ แล้วภายหลังมีการแปล/เขียน `prompt_templates.py` ใหม่เป็นภาษาไทย แต่ลืมอัปเดต string ตัวแบ่งใน `NoLLM.chat()` ให้ตรงกัน — เป็นตัวอย่างคลาสสิกของ "โค้ดสองจุดที่ผูกกันด้วย string คงที่ (hard-coded marker) แต่ไม่ได้ sync กันเมื่อจุดใดจุดหนึ่งถูกแก้"

**วิธีตรวจสอบ**: ตั้ง `USE_LLM = False` แล้วถามคำถามที่รู้อยู่แล้วว่ามีคำตอบในฐานความรู้แน่นอน ถ้าระบบตอบว่า "ไม่พบข้อมูลที่เกี่ยวข้อง" ทุกครั้งแม้จะเป็นคำถามที่ควรตอบได้ ก็ยืนยันได้ว่า bug นี้เกิดขึ้นจริง

**การแก้ไข**: มี 2 แนวทาง (1) แก้ marker string ใน `NoLLM.chat()` ให้ตรงกับ `USER_PROMPT` จริง คือเปลี่ยนเป็น `.split("ข้อมูลอ้างอิง:")` และ `.split("คำถามของผู้ใช้:")` หรือ (2) วิธีที่ดีกว่าเชิงสถาปัตยกรรม คือให้ `NoLLM` ไม่ต้องไปแกะข้อความจาก prompt string เลย แต่ให้ `Generator.generate()` ส่ง `chunks` (list ของ chunk ที่ค้นเจอ) เข้าไปให้ `NoLLM` ใช้ตรง ๆ แทน เพราะข้อมูลนั้นมีอยู่แล้วในโค้ดชั้นบน ไม่จำเป็นต้อง parse กลับจาก string ที่ประกอบไว้แล้ว — วิธีนี้ทำให้ทั้งสองส่วนไม่ผูกกันด้วย string คงที่อีกต่อไป (ลด coupling)

### 3.3 ข้อสังเกตเสริม: README กับโค้ดจริงของ DL-05 ไม่ตรงกัน

`main.py` ของ DL-05 import โมดูล `problem10_debug_scripts` และมีเมนูข้อ 10 "Debug RAG Scripts" แต่ในโฟลเดอร์ไม่มีไฟล์ `problem10_debug_scripts.py` อยู่จริง (มีแค่ `problem01`–`problem09`) และ `README.md` เองก็สรุปไว้แค่ 9 หัวข้อ ไม่มีข้อ 10 เช่นกัน หมายความว่าการรัน `python main.py` ของ DL-05 จะ **ล้มทันทีตั้งแต่ import บรรทัดแรก ๆ** ด้วย `ModuleNotFoundError: No module named 'problem10_debug_scripts'` ก่อนจะแสดงเมนูด้วยซ้ำ นี่ไม่ใช่ปัญหาของ RAG โดยตรง แต่เป็นตัวอย่างของปัญหาระดับ "ความสอดคล้องระหว่างเอกสาร (README) กับโค้ดจริง" (documentation drift) ที่ควรรู้จักไว้เผื่อถูกถามเรื่องกระบวนการพัฒนาซอฟต์แวร์ควบคู่ไปด้วย

---

## 4. ตาราง Config: ความหมายและผลของการปรับค่าแต่ละตัว

ตารางนี้อ้างอิงจาก `config.py` ของโปรเจกต์หลัก (DL-04) ทุกค่า ตัวแปรถูกจัดกลุ่มตามหน้าที่เพื่อให้เห็นว่าอยู่ตรงไหนของ pipeline

### 4.1 สวิตช์เปิด/ปิดฟีเจอร์หลัก

| ตัวแปร | ความหมาย | ผลเมื่อเปิด (True) | ผลเมื่อปิด (False) |
|---|---|---|---|
| `USE_HYBRID` | ค้นด้วย BM25 ควบคู่กับ dense | เจอคำเฉพาะ/ตัวย่อ/คำที่พิมพ์ตรงกับเอกสารได้ดีขึ้น โดยเฉพาะ query แบบ `partial` | ใช้ dense อย่างเดียว เร็วกว่าเล็กน้อยแต่พลาด exact-match ได้ง่ายขึ้น (ดูข้อ 2.6) |
| `USE_RERANK` | จัดอันดับใหม่ด้วย cross-encoder | แม่นยำขึ้นมาก โดยเฉพาะ MRR/nDCG แต่ **ช้าลงมาก** เพราะต้องโหลดโมเดลใหญ่และรันทีละคู่ query-document | เร็วกว่า แต่เอกสารที่ตรงประเด็นเฉพาะทางอาจตกอันดับ (ดูข้อ 2.9) |
| `USE_QUERY_TRANSFORM` | ใช้ LLM ปรับคำถามก่อนค้น | รับมือคำถามกำกวม/ไม่สมบูรณ์ได้ดีขึ้น แต่ **เสีย LLM เพิ่ม 1 ครั้งต่อคำถาม** (เวลา+ค่าใช้จ่ายเพิ่ม) | ใช้แค่ `normalize_query()` (แทนคำแสลงตามตาราง) ซึ่งฟรีและเร็วเสมอ |
| `USE_MEMORY` | จำบทสนทนาไว้ตอบคำถามต่อเนื่อง | ตอบคำถามแบบ follow-up ได้ ("แล้วผลข้างเคียงล่ะ") แต่ prompt ยาวขึ้นตามจำนวนรอบสนทนา | แต่ละคำถามเป็นอิสระจากกันโดยสิ้นเชิง เหมาะกับการทดสอบ/ประเมินผล (เพราะ `eval_generation.py` จะปิดค่านี้ชั่วคราวเสมอ) |
| `USE_LLM` | เรียก LLM เพื่อเขียนคำตอบ | ได้คำตอบที่เรียบเรียงเป็นธรรมชาติ พร้อมอ้างอิง `[n]` | แสดงข้อความดิบที่ค้นเจอแทน (ตามทฤษฎี — ปัจจุบันมี bug ตามข้อ 3.2 ทำให้โหมดนี้คืน "ไม่พบข้อมูล" เสมอ) ใช้สำหรับทดสอบ retrieval แยกจาก generation |
| `SHOW_SOURCES` | แสดงรายการแหล่งอ้างอิงท้ายคำตอบ | ผู้ใช้เห็นว่าคำตอบมาจาก chunk ไหนบ้าง (ตรวจสอบได้) | คำตอบสั้นกระชับกว่า แต่ตรวจสอบที่มาไม่ได้จากหน้าจอ |
| `SHOW_DEBUG` | แสดงคะแนน/เวลาแต่ละขั้น | เห็น query ที่ใช้ค้นจริงและเวลาแต่ละ stage ช่วย debug performance | หน้าจอสะอาด เหมาะกับ end-user |

### 4.2 การเตรียมข้อมูล (ต้องรัน `build_index.py` ใหม่เสมอเมื่อแก้ค่าในกลุ่มนี้)

| ตัวแปร | ความหมาย | ผลเมื่อปรับค่า |
|---|---|---|
| `CHUNK_SIZE` | จำนวนตัวอักษรสูงสุดต่อ 1 chunk (ปัจจุบัน 400) | เพิ่มขึ้น → คำตอบยาว ๆ ถูกตัดเป็นชิ้นน้อยลง แต่ละ chunk มีบริบทมากขึ้นแต่เสี่ยงเจือจางถ้ารวมหลายประเด็น (ดูข้อ 2.3); ลดลง → เสี่ยงตัดกลางคำอธิบายมากขึ้น เพราะ dataset นี้คำตอบส่วนใหญ่สั้นกว่า 400 อยู่แล้ว การเพิ่มค่านี้จึงมีผลน้อยกว่าการลดค่าลงต่ำมาก ๆ |
| `CHUNK_OVERLAP` | จำนวนตัวอักษรที่ chunk ติดกันซ้อนทับกัน (ปัจจุบัน 50) | เพิ่มขึ้น → กันใจความขาดตอนตรงรอยต่อได้ดีขึ้น แต่เพิ่มความซ้ำซ้อนของข้อมูลในดัชนี (ดัชนีใหญ่ขึ้น); ลดลงเหลือ 0 → ไม่มีการกันขอบเขตเลย เสี่ยงขาดใจความสำหรับคำตอบที่ยาวเกิน `CHUNK_SIZE` |
| `EMBEDDING_MODEL_NAME` | โมเดลแปลงข้อความเป็นเวกเตอร์ (ปัจจุบัน `paraphrase-multilingual-MiniLM-L12-v2`) | เปลี่ยนโมเดล → มิติเวกเตอร์และ "พื้นที่ความหมาย" เปลี่ยนไปทั้งหมด **ต้องรัน `build_index.py` ใหม่เสมอ** ดัชนีเก่าใช้กับโมเดลใหม่ไม่ได้ (ถูกตรวจจับโดย `index_meta.py` เพราะอยู่ใน `TRACKED_SETTINGS`) โมเดลใหญ่ขึ้นมักแม่นขึ้นแต่ช้าลงและใช้ RAM มากขึ้น |

### 4.3 การค้นหา

| ตัวแปร | ความหมาย | ผลเมื่อปรับค่า |
|---|---|---|
| `TOP_K` | จำนวน chunk สุดท้ายที่ส่งให้ LLM เขียนคำตอบ (ปัจจุบัน 3) | เพิ่มขึ้น → LLM เห็นบริบทกว้างขึ้น ตอบครบถ้วนขึ้น แต่เสี่ยง prompt ยาวและมี "สัญญาณรบกวน" จาก chunk ที่ไม่เกี่ยวข้องปนเข้ามา; ลดลง → คำตอบโฟกัสมากขึ้นแต่เสี่ยงพลาดข้อมูลสำคัญที่อยู่นอก top-k |
| `CANDIDATE_K` | จำนวนผู้เข้ารอบก่อนรวมผล/ก่อน rerank (ปัจจุบัน 20) | ต้อง **มากกว่า** `TOP_K` เสมอ โดยเฉพาะเมื่อเปิด `USE_RERANK` เพราะต้องมีตัวเลือกให้ cross-encoder จัดอันดับใหม่ ค่าน้อยเกินไปทำให้ rerank ไม่มีประโยชน์ (ไม่มีผู้เข้ารอบสำรองให้เลือก) ค่ามากเกินไปทำให้ rerank ช้าลงเพราะต้องประเมินคู่มากขึ้น |
| `RRF_K` | ค่าคงที่ในสูตร RRF (ปัจจุบัน 60) | ค่าน้อย → อันดับต้น ๆ ของแต่ละวิธีมีอิทธิพลเหนือกว่ามาก (winner-take-most); ค่ามาก → คะแนนระหว่างอันดับต่าง ๆ ใกล้เคียงกันมากขึ้น (ถ่วงน้ำหนักเท่ากันมากขึ้น) 60 เป็นค่ามาตรฐานที่งานวิจัยต้นฉบับแนะนำ |
| `RERANK_MODEL_NAME` | โมเดล cross-encoder ที่ใช้เมื่อ `USE_RERANK=True` | เปลี่ยนโมเดล → ความแม่นยำ/ความเร็วเปลี่ยนไปตามขนาดโมเดล ไม่ต้อง rebuild index เพราะ reranker ทำงานตอน query time ไม่ใช่ index time |
| `QUERY_TRANSFORM_MODE` | โหมดของ query transform (`rewrite` / `multi_query` / `hyde`) | `rewrite` เหมาะกับคำถามต่อเนื่องที่ต้องเติมบริบท; `multi_query` เหมาะกับเพิ่ม recall โดยค้นหลายมุมมองพร้อมกัน; `hyde` เหมาะกับคำถามสั้นมาก ๆ ที่ขาดคำสำคัญ แต่เสี่ยงหลุดประเด็นถ้า LLM แต่งคำตอบผิด |
| `MULTI_QUERY_COUNT` | จำนวนคำถามทางเลือกที่สร้างในโหมด `multi_query` | เพิ่มขึ้น → ค้นครอบคลุมมุมมองมากขึ้นแต่ค้นหาช้าลง (ต้องรัน dense+BM25 search ซ้ำต่อ 1 query ที่เพิ่มมา) |

### 4.4 LLM

| ตัวแปร | ความหมาย | ผลเมื่อปรับค่า |
|---|---|---|
| `LLM_PROVIDER` | ผู้ให้บริการ LLM (`ollama` / `openai` / `gemini`) | เปลี่ยนค่านี้จะสลับ `base_url` และโมเดล default ทั้งหมดตาม `LLM_PROVIDERS` dict; `ollama` รันบนเครื่องตัวเอง ไม่ต้องใช้ API key แต่ต้องมีเครื่องแรงพอ ส่วน `openai`/`gemini` ต้องตั้งค่า environment variable ของ API key ตามชื่อใน `LLM_PROVIDERS` (`OPENAI_API_KEY`, `GOOGLE_API_KEY`) |
| `LLM_MODEL` | ชื่อโมเดลเจาะจง (ค่าว่าง = ใช้ default ของ provider) | ระบุเอง → คุมคุณภาพ/ราคา/ความเร็วได้ละเอียดขึ้น เช่นเปลี่ยนจาก `llama3.1:8b` เป็นโมเดลอื่นที่ ollama รองรับ |
| `LLM_TEMPERATURE` | ความสุ่มของคำตอบ (ปัจจุบัน 0.2) | ค่าต่ำ → คำตอบนิ่ง ตรงตาม context มากขึ้น (ลดความเสี่ยง faithfulness ผิดเพี้ยนตามข้อ 2.10); ค่าสูง → คำตอบหลากหลาย/สร้างสรรค์ขึ้นแต่เสี่ยงบิดเบือนรายละเอียดสำคัญมากขึ้น ในโดเมนสุขภาพจึงควรตั้งต่ำเสมอ |
| `LLM_MAX_TOKENS` | ความยาวสูงสุดของคำตอบ | เพิ่มขึ้น → ตอบได้ละเอียด/ยาวขึ้น แต่ช้าและแพงขึ้น; ลดลงมากเกินไปเสี่ยงคำตอบถูกตัดกลางประโยค |

### 4.5 ข้อความและการประเมินผล

| ตัวแปร | ความหมาย | ผลเมื่อปรับค่า |
|---|---|---|
| `MEMORY_MAX_TURNS` | จำนวนรอบสนทนาที่จำได้ (ปัจจุบัน 6) | เพิ่มขึ้น → จำบริบทได้นานขึ้นแต่ prompt ยาวขึ้นทุกคำถาม (ค่าใช้จ่าย/เวลาเพิ่มสะสม); ลดลง → ประหยัดขึ้นแต่เสี่ยงลืมบริบทเร็ว (แม้จะยังมี `first_topic` กันไว้บางส่วนตามข้อ 2.11) |
| `NO_CONTEXT_MESSAGE` | ข้อความเมื่อไม่พบข้อมูล | เปลี่ยนคำพูดได้ตามโทนของระบบ แต่ต้องระวังให้ตรงกับ marker string ที่ใช้เช็คใน `is_refusal()` ของ `eval_generation.py` (เช็คจากวลี `"ไม่พบข้อมูล"`, `"ไม่มีข้อมูล"`, `"ไม่สามารถตอบ"`) ถ้าแก้ข้อความจนไม่มีวลีเหล่านี้ ตัวชี้วัด "อัตราการตอบว่าไม่รู้" จะรายงานผิดพลาด |
| `EVAL_K_VALUES` | ค่า k ที่ใช้วัด Hit/Recall/Precision/nDCG (ปัจจุบัน `[1,3,5,10]`) | เพิ่มค่า k มากขึ้น → เห็นภาพว่าระบบ "หาเจอบ้างไหมถ้าดูลึกลงไปอีก" (recall กว้างขึ้น) แต่ไม่สะท้อนประสบการณ์ผู้ใช้จริงเพราะผู้ใช้เห็นแค่ `TOP_K` อันดับแรกเท่านั้น ควรมีอย่างน้อยหนึ่งค่าที่เท่ากับ `TOP_K` จริงเพื่อวัดสิ่งที่ผู้ใช้เห็นจริง |
| `GOLDEN_SET_SIZE` | จำนวนข้อในชุดทดสอบที่สุ่มสร้าง (ปัจจุบัน 60) | เพิ่มขึ้น → ผลประเมินแม่นยำ/น่าเชื่อถือขึ้น (variance ต่ำลง) แต่ประเมินช้าลงโดยเฉพาะ `eval_generation.py` ที่ต้องเรียก LLM ทุกข้อ; ลดลงมากเกินไปเสี่ยงสรุปผลคลาดเคลื่อนจากการสุ่มตัวอย่างน้อยไป |

---

## 5. แนวคิดพื้นฐาน RAG/LLM สำหรับข้อสอบปรนัย

ส่วนนี้แยกจากส่วนที่ 1–4 โดยสิ้นเชิง — ไม่ผูกกับโปรเจกต์ตัวอย่างหรือ "ระบบที่พัฒนาขึ้น" แต่เป็นแนวคิดพื้นฐานของ RAG/LLM ที่ข้อสอบปรนัยมักถาม รวบรวมในรูปแบบคำถาม-คำตอบ-เหตุผล เพื่อให้ฝึกแยกแยะตัวลวง (distractor) ในตัวเลือกแต่ละข้อได้

### 5.1 Token และ Context Window

**คำถามตัวอย่าง**: เหตุใดจำนวน Token จึงมีผลต่อระบบ LLM?
ก. มีผลต่อ Vocabulary และจำนวนคำที่ Tokenizer สามารถแยกได้
ข. มีผลต่อ Context Window, Processing Time, Cost และ Memory Usage
ค. มีผลต่อ Model Parameters และจำนวน Layer ที่ใช้ประมวลผล
ง. มีผลต่อ Embedding Dimension และขนาด Vector ของแต่ละ Token
จ. มีผลต่อ Sampling และจำนวนคำตอบที่โมเดลสามารถสร้างได้

**คำตอบ: ข.**

Token เป็นหน่วยพื้นฐานที่ LLM ใช้จำกัดขนาด context ที่รับได้ในคราวเดียว (context window) ยิ่งจำนวน token ที่ป้อนเข้า/ประมวลผลมากขึ้น กลไก self-attention ต้องคำนวณความสัมพันธ์ระหว่างทุกคู่ token (มีความซับซ้อนแบบไม่เชิงเส้นตามจำนวน token) ทำให้ processing time นานขึ้นและใช้ memory (VRAM/RAM) มากขึ้นตามไปด้วย และถ้าเป็นการเรียกผ่าน API ที่คิดค่าใช้จ่ายตามจำนวน token (เช่น `LLM_MAX_TOKENS` ใน config ของโปรเจกต์ตัวอย่าง) ก็มีผลต่อ cost โดยตรงเช่นกัน

เหตุผลที่ตัวเลือกอื่นผิด: **ก.** vocabulary/tokenizer ถูกกำหนดตายตัวตั้งแต่ตอนเทรนโมเดล ไม่เปลี่ยนตามจำนวน token ที่ป้อนเข้าขณะใช้งาน **ค.** จำนวน parameter/layer เป็นคุณสมบัติของสถาปัตยกรรมโมเดลที่ตายตัว ไม่ได้แปรผันตามความยาวอินพุต **ง.** embedding dimension คงที่ตามการออกแบบโมเดล ไม่ขึ้นกับจำนวน token ที่ป้อน **จ.** sampling (temperature, top-p) เป็นกลไกเลือกคำตอบ ไม่ได้เกี่ยวข้องกับจำนวน token โดยตรง

โยงกับโปรเจกต์ตัวอย่าง: `MEMORY_MAX_TURNS` ที่ควบคุมว่าจำบทสนทนากี่รอบ (`memory.py`) และการจำกัด `max_chars` ใน `format_context()` (`prompt_templates.py`) ล้วนเป็นกลไกป้องกันไม่ให้จำนวน token ของ prompt บวมจนเกิน context window หรือทำให้ค่าใช้จ่าย/เวลาตอบพุ่งขึ้นโดยไม่จำเป็น

### 5.2 จุดประสงค์ของการทำ Chunking

**คำถามตัวอย่าง**: การแบ่งเอกสารขนาดใหญ่เป็น Chunk มีวัตถุประสงค์สำคัญที่สุดข้อใด?
ก. เพื่อเพิ่มจำนวน Document ให้ระบบมี Knowledge Base ขนาดใหญ่ขึ้น
ข. เพื่อทำให้แต่ละส่วนของ Document มี Semantic Meaning เหมือนกัน
ค. เพื่อให้ข้อความมีขนาดเหมาะสมต่อ Embedding และ Retrieval ที่เกี่ยวข้อง
ง. เพื่อปรับ Embedding Dimension ให้เหมาะกับขนาดของแต่ละ Document
จ. เพื่อเพิ่ม Context Window ให้ LLM รองรับ Document ได้มากขึ้น

**คำตอบ: ค.**

เป้าหมายหลักของ chunking คือให้แต่ละชิ้นมีขนาดพอดี ไม่ใหญ่จนเจือจางความหมาย (embedding ครอบคลุมหลายประเด็นปนกันจนไม่โฟกัส) และไม่เล็กจนขาดบริบท (ตัดกลางประโยคอธิบาย) เพื่อให้ทั้งขั้นตอนสร้าง embedding และขั้นตอน retrieval ทำงานได้แม่นยำที่สุด ดูตัวอย่างจริงในหัวข้อ 2.3 ของเอกสารนี้ (`text_splitter.py` และ `problem04_chunking.py`)

เหตุผลที่ตัวเลือกอื่นผิด: **ก.** chunking ไม่ได้เพิ่ม "ความรู้ใหม่" เพียงแค่แบ่งของเดิมที่มีอยู่แล้วออกเป็นชิ้นย่อย จำนวนความรู้จริงไม่ได้เพิ่มขึ้น **ข.** แต่ละ chunk ไม่จำเป็นต้องมี semantic meaning เหมือนกัน — ตรงกันข้าม การมี chunk ที่ปนหลายความหมายเข้าด้วยกันคือปัญหาที่ chunking ที่ดีควรหลีกเลี่ยง (ดูปัญหา "chunk ใหญ่เกินไป" ในหัวข้อ 2.3) **ง.** embedding dimension เป็นค่าคงที่ของโมเดล (กำหนดตอนเทรน) ไม่ได้ถูกปรับตามขนาดเอกสารหรือ chunk **จ.** chunking เกิดขึ้นตอนสร้าง index (indexing time) เป็นคนละขั้นตอนกับ context window ของ LLM ตอน generate (query time) ไม่ได้ทำให้ context window ของ LLM ใหญ่ขึ้นแต่อย่างใด

### 5.3 หลักการแยกแยะตัวลวงในข้อสอบปรนัยแนวนี้

จากสองตัวอย่างข้างต้น จะเห็นรูปแบบตัวลวงที่ข้อสอบมักใช้ซ้ำ ๆ คือ

1. **สลับคุณสมบัติของโมเดลกับคุณสมบัติของอินพุต** — เช่น อ้างว่าจำนวน token มีผลต่อ vocabulary/parameter/embedding dimension ทั้งที่สิ่งเหล่านี้ถูกกำหนดตายตัวตั้งแต่ตอนออกแบบ/เทรนโมเดล ไม่เปลี่ยนตามอินพุตที่ป้อนเข้าขณะใช้งาน
2. **สลับขั้นตอน (stage) ของ pipeline** — เช่น อ้างว่า chunking (เกิดตอน index) มีผลต่อ context window (เกิดตอน generate) ทั้งที่เป็นคนละขั้นตอน คนละช่วงเวลากันโดยสิ้นเชิง วิธีป้องกันตัวเองคือให้นึกภาพ pipeline ในหัวข้อ 1 เสมอ แล้วถามว่า "แนวคิดที่โจทย์พูดถึง เกิดขึ้นที่ขั้นไหนของ pipeline กันแน่"
3. **ใช้คำที่ฟังดูถูกแต่ผิดขอบเขต** — เช่น "เพื่อเพิ่ม Knowledge Base ให้ใหญ่ขึ้น" ฟังดูเกี่ยวข้องกับ RAG แต่ผิดเพราะสลับ "การจัดระเบียบข้อมูลเดิม" กับ "การเพิ่มข้อมูลใหม่" ซึ่งเป็นคนละเรื่องกัน

หากมีโจทย์ปรนัยเพิ่มเติมจากอาจารย์ สามารถนำมาวิเคราะห์เพิ่มในรูปแบบเดียวกันนี้ได้

---

## 6. กรณีศึกษาจากระบบที่พัฒนาขึ้นจริงของตนเอง (Football Trivia RAG)

โจทย์บรรยายของอาจารย์ระบุให้อธิบาย "ให้สอดคล้องกับการทำงานและ Source Code ของระบบ RAG ที่นักศึกษาพัฒนาขึ้นจริง" ส่วนที่ 1–5 ข้างต้นวิเคราะห์จากโปรเจกต์ตัวอย่างของอาจารย์ (โดเมนสุขภาพทางเพศ) แต่ส่วนนี้วิเคราะห์จาก **repository ส่วนตัวที่พัฒนาต่อจริง** (`04622404-Advanced-Topics-in-Computer-Software`) ซึ่งนำโครง pipeline เดียวกันไปปรับใช้กับโดเมนใหม่คือ "ควิซความรู้รอบตัวฟุตบอล" (`data/football_trivia_qa.txt` แปลงจาก `trivia_questions.csv` มี 2,022 chunk ตาม `index_meta.json`) และปรับหลายอย่างให้เป็นของตัวเอง คือเปลี่ยนผู้ให้บริการ LLM เป็น **Groq** (โมเดล `openai/gpt-oss-120b`) เพิ่ม provider ใหม่เข้าไปใน `LLM_PROVIDERS` เอง ตั้ง `LLM_TEMPERATURE = 0.0` (เข้มกว่าเทมเพลตต้นฉบับที่ 0.2 — สมเหตุสมผลเพราะคำถามแบบ trivia มีคำตอบถูกต้องตายตัวเพียงคำตอบเดียว ต่างจากคำถามสุขภาพที่ต้องใช้น้ำเสียงสนทนา) เปลี่ยน `SLANG_MAP` เป็นศัพท์แสลงวงการฟุตบอล (`goalie`→`goalkeeper`, `ref`→`referee`, `VAR`, `UCL`→`UEFA Champions League` ฯลฯ) และเปิดใช้ **ทุกฟีเจอร์ในไปป์ไลน์พร้อมกัน** (`USE_HYBRID`, `USE_RERANK`, `USE_QUERY_TRANSFORM`, `USE_MEMORY`, `USE_LLM`, `SHOW_SOURCES`, `SHOW_DEBUG` ตั้งเป็น `True` ทั้งหมด) ต่างจากค่าเริ่มต้นของเทมเพลตที่เปิดไม่ครบ

ต่อไปนี้คือปัญหาจริงที่ตรวจพบจากการอ่านซอร์สโค้ดฉบับนี้โดยตรง (เทียบไฟล์ config.py กับ main.py/prompt_templates.py ของ repo เดียวกัน) ซึ่งเป็นตัวอย่างที่ตรงโจทย์ "ปัญหาที่เกิดขึ้นจริง...สอดคล้องกับระบบที่นักศึกษาพัฒนาขึ้นจริง" มากที่สุด เพราะเป็นโค้ดชุดที่ใช้งานจริง ไม่ใช่การจำลอง

### 6.1 เปิด `SHOW_SOURCES = True` แล้วไม่มีผลใด ๆ เลย (bug ที่ตรวจสอบได้จริง)

ใน `config.py` ของระบบนี้ตั้งค่า `SHOW_SOURCES = True` ไว้อย่างตั้งใจ (คอมเมนต์บอกว่า "True = แสดงรายการแหล่งอ้างอิงท้ายคำตอบ") แต่พอไปดู `main.py` ที่เป็นตัวแสดงผลจริง กลับพบว่าโค้ดส่วนที่อ่านค่านี้ไปพิมพ์แหล่งอ้างอิง **ถูกคอมเมนต์ปิดทิ้งไว้ทั้งบล็อก**:

```python
def print_answer(result):
    print()
    print(result["answer"])

    #if config.SHOW_SOURCES and result["sources"]:
    #    print("\nSources:")
    #    for source in result["sources"]:
    #        print(f"  [{source['n']}] {source['question']}")
    #        print(f"      Line {source['line_no']} · Score {source['score']}")

    if config.SHOW_DEBUG:
        ...
```

**ผลกระทบ**: ไม่ว่าจะตั้ง `SHOW_SOURCES` เป็น `True` หรือ `False` ก็ตาม หน้าจอจะไม่แสดงรายการแหล่งอ้างอิงเลยสักครั้ง เพราะโค้ดที่ควรอ่านค่านี้ไปใช้ถูก comment out ไว้ตั้งแต่ต้น ตัวแปร config ตัวนี้จึง "ไม่มีผลจริง" ต่อพฤติกรรมของโปรแกรม (dead configuration) ทั้งที่ `result["sources"]` ถูกสร้างไว้พร้อมใช้งานแล้วจริง ๆ ใน `Generator.build_sources()` (`src/generator.py`)

**วิธีตรวจสอบ**: รันโปรแกรม ถามคำถามที่มีคำตอบแน่นอน (เช่นในตัวอย่าง data — "Who scored the first ever goal in the English Premier League?") แล้วสังเกตว่าไม่มีส่วน "Sources:" ปรากฏท้ายคำตอบเลย แม้ `SHOW_SOURCES = True` ทั้งที่ `SHOW_DEBUG` (บล็อกถัดไปในไฟล์เดียวกัน) แสดงผลตามค่าที่ตั้งไว้ปกติ — เป็นการเทียบพฤติกรรมของสองสวิตช์ที่อยู่ติดกันในไฟล์เดียวกันแต่ทำงานต่างกัน

**สาเหตุ**: เป็นร่องรอยจากการที่ผู้พัฒนาคอมเมนต์โค้ดส่วนนี้ไว้ตอนดีบัก/ทดสอบ แล้วลืมเปิดกลับคืนตอนไปตั้งค่า `SHOW_SOURCES = True` ใน `config.py` ในภายหลัง — เป็นตัวอย่างคลาสสิกของปัญหาที่ config ตัวหนึ่งกับโค้ดที่ใช้ค่านั้น "หลุด sync กัน" คล้ายกับปัญหา marker string ใน `NoLLM.chat()` ที่พบในเทมเพลตของอาจารย์ (ข้อ 3.2) เพียงแต่ครั้งนี้เกิดในโค้ดของตัวเองและเป็นสวิตช์ที่ตั้งใจเปิดใช้งานจริง

**การแก้ไข**: เอาเครื่องหมาย `#` ออกจากบล็อกที่พิมพ์ `Sources:` ใน `print_answer()` เพื่อให้ `SHOW_SOURCES` มีผลจริงตามที่ตั้งใจไว้ และควรเพิ่มการทดสอบง่าย ๆ (manual smoke test) ทุกครั้งหลังแก้ config ว่าพฤติกรรมที่เห็นจริงตรงกับคอมเมนต์ที่อธิบายไว้หรือไม่

### 6.2 `requirements.txt` ยังขาด dependency เดิมที่ไม่เคยแก้

`requirements.txt` ของระบบนี้อัปเดตแล้วบางส่วน (เพิ่ม `openai` และ `python-dotenv` เข้ามาเพราะเปลี่ยนไปใช้ Groq ผ่าน `openai` client และใช้ `.env` เก็บ API key) แต่ **`rank_bm25` และ `pythainlp`** ที่ `src/hybrid_retriever.py` เรียกใช้อยู่ (สำหรับ BM25 index และการตัดคำ) ยัง **ไม่ถูกเพิ่มเข้าไป** เหมือนกับที่พบในเทมเพลตต้นฉบับ (ข้อ 3.1) เพราะมี `.venv` ติดตั้งไว้ในเครื่องของผู้พัฒนาเองอยู่แล้ว (เห็นโฟลเดอร์ `.venv` ที่ root ของ repo) ทำให้รันได้ปกติบนเครื่องตัวเอง แต่ถ้ามีคนอื่น clone repo นี้ไปแล้วติดตั้งตาม `requirements.txt` อย่างเดียว โปรแกรมจะ crash ทันทีด้วย `ModuleNotFoundError: No module named 'rank_bm25'` — เป็นตัวอย่างของปัญหา "ใช้ได้บนเครื่องฉัน" (works on my machine) ที่เกิดจากการพึ่งพา environment ที่ติดตั้งไว้ก่อนแล้ว โดยไม่ได้ล็อกรายการ dependency ให้ตรงกับสิ่งที่โค้ดใช้จริงทุกครั้งที่แก้ไขโค้ด

### 6.3 ภาษาปนกันในคำตอบ (English/Thai mixed output)

ระบบนี้แปล `SYSTEM_PROMPT`, `USER_PROMPT`, และ prompt ย่อยทั้งหมดใน `prompt_templates.py` เป็นภาษาอังกฤษล้วนเพื่อให้เหมาะกับโดเมนฟุตบอลที่เป็นภาษาอังกฤษ แต่ใน `config.py` สองค่าที่ยังไม่ได้แปลคือ

```python
NO_CONTEXT_MESSAGE = "ขออภัย ไม่พบข้อมูลที่เกี่ยวข้อง"
DISCLAIMER = "หมายเหตุ: ข้อมูลนี้มาจากฐานข้อมูลตอบคำถามฟุตบอล"
```

**ผลกระทบ**: `Generator.generate()` (`src/generator.py`) ต่อท้ายคำตอบทุกคำตอบด้วย `config.DISCLAIMER` เสมอ (`if config.DISCLAIMER not in answer: answer = f"{answer}\n\n{config.DISCLAIMER}"`) หมายความว่า **ทุกคำตอบที่ LLM เขียนเป็นภาษาอังกฤษล้วน จะมีประโยคภาษาไทยแปะต่อท้ายเสมอ** ทำให้ผลลัพธ์ที่ผู้ใช้เห็นเป็นภาษาปนกันอย่างไม่ตั้งใจ เช่นเดียวกัน ถ้าค้นหาไม่เจอ context เลย ระบบจะตอบด้วย `NO_CONTEXT_MESSAGE` ภาษาไทยแทรกอยู่กลางบทสนทนาภาษาอังกฤษทั้งหมด

**สาเหตุ**: การแปลระบบจากภาษาไทยเป็นภาษาอังกฤษทำแบบไม่ครบทุกจุด — แปลเฉพาะ prompt ที่ส่งให้ LLM แต่ลืมแปล string คงที่ (constant) อีก 2 ตัวที่อยู่ใน config คนละไฟล์ ซึ่งเป็นความเสี่ยงที่เกิดขึ้นได้ง่ายเมื่อข้อความของระบบไม่ได้รวมศูนย์ไว้ที่เดียว

**วิธีตรวจสอบ**: ถามคำถามที่รู้คำตอบแน่นอน แล้วดูว่าท้ายคำตอบมีประโยคภาษาไทยแปะมาด้วยหรือไม่ หรือถามคำถามที่ไม่มีในฐานข้อมูลเพื่อดูว่าข้อความปฏิเสธเป็นภาษาไทยหรือไม่ ทั้งที่ระบบทั้งหมดควรเป็นภาษาอังกฤษ

**การแก้ไข**: แปล `NO_CONTEXT_MESSAGE` และ `DISCLAIMER` เป็นภาษาอังกฤษให้สอดคล้องกับ `prompt_templates.py` (เช่น `"Sorry, no relevant information was found."` และ `"Note: This information is from the football trivia database."`) และควรพิจารณาต่อไปว่าถ้าต้องรองรับหลายภาษาในอนาคต ควรรวมข้อความของระบบไว้ที่ไฟล์เดียว (เช่น locale/messages file) แทนการกระจายอยู่หลายไฟล์เพื่อลดความเสี่ยงแบบนี้

### 6.4 คอมเมนต์และ docstring ที่หลงเหลือจากโดเมนเดิม (documentation drift)

ไฟล์หลายไฟล์ที่ไม่ได้ถูกแก้ไข logic ยังมีคอมเมนต์ที่อ้างถึงโดเมนสุขภาพทางเพศเดิมอยู่ ทั้งที่ตัวระบบเปลี่ยนไปทำงานกับฟุตบอลแล้วจริง เช่น `src/document_loader.py` บรรทัดคอมเมนต์บนสุดยังเขียนว่า `"Load data/sex_q_a.txt and convert it into a list of Q&A records"` ทั้งที่ `config.SOURCE_FILE` ชี้ไปที่ `football_trivia_qa.txt` จริง และ `src/retriever.py` กับ `src/rag_pipeline.py` ยังมีตัวอย่างการใช้งานในคอมเมนต์เป็น `rag.ask("What should I do if a condom breaks?")` ส่วน `src/query_transform.py` ทั้งโมดูลยังมีคำอธิบายปัญหาด้วยตัวอย่าง `"I have a sore on my private part" ← slang, Knowledge base: "penis"` ซึ่งไม่เกี่ยวข้องกับฟุตบอลเลย

**นี่ไม่ใช่บั๊กที่ทำให้โปรแกรมทำงานผิด** (โค้ดจริงทำงานถูกต้องตามข้อมูลฟุตบอล) แต่เป็นปัญหาประเภท **maintainability** — คอมเมนต์ที่ผิดจากพฤติกรรมจริงของโค้ดจะทำให้คนที่มาอ่านทีหลัง (รวมถึงตัวเองในอีกไม่กี่เดือน) เข้าใจผิดว่าระบบยังทำงานกับข้อมูลสุขภาพอยู่ หรือสับสนเมื่อพยายามหาไฟล์ `sex_q_a.txt` ที่ไม่มีอยู่จริงในโปรเจกต์นี้แล้ว เป็นผลข้างเคียงที่พบได้เสมอเมื่อ "clone เทมเพลตของอาจารย์มาแล้วเปลี่ยนแค่ข้อมูล/ค่า config แต่ไม่ได้ไล่อัปเดตคอมเมนต์ในทุกไฟล์ตามไปด้วย" — ควรไล่ grep คำว่า `sex_q_a`, `condom`, `penis` ทั่ว repository แล้วแก้คอมเมนต์ให้ตรงกับโดเมนจริงก่อนส่งงาน

### 6.5 `load_dotenv()` ถูกเรียกซ้ำโดยไม่จำเป็น

`config.py` เรียก `load_dotenv()` สองครั้ง — ครั้งแรกที่บรรทัดบนของไฟล์ (`from dotenv import load_dotenv` แล้วเรียกทันที บรรทัด 11–13) และอีกครั้งถูกครอบด้วย `try/except ImportError` ไว้อีกทีถัดไปไม่กี่บรรทัด (บรรทัด 33–37) ทั้งที่ `dotenv` ถูก import สำเร็จไปแล้วตั้งแต่บรรทัดแรก การครอบ `try/except ImportError` รอบที่สองจึงเป็น **dead code ที่ไม่มีทางถูก except ได้เลย** ในทางปฏิบัติไม่ทำให้ระบบพัง เพราะ `load_dotenv()` เรียกซ้ำได้โดยไม่มีผลข้างเคียงร้ายแรง (แค่โหลดค่าเดิมทับอีกครั้ง) แต่เป็นตัวอย่างของโค้ดที่ถูกแก้ไขหลายรอบ (อาจแก้ตอนแรกแบบหนึ่ง แล้วมาเพิ่ม `.env` โหลดจาก path เฉพาะ `BASE_DIR` อีกรอบ) แล้วลืมลบของเดิมทิ้ง ควรรวมให้เหลือจุดเดียว โดยระบุ path ของ `.env` ให้ชัดเจนตั้งแต่ครั้งแรกไปเลย

### 6.6 เปิดทุกฟีเจอร์พร้อมกัน: ผลต่อ latency ที่ต้องเจอจริง

เมื่อเปิด `USE_QUERY_TRANSFORM`, `USE_HYBRID`, `USE_RERANK`, และ `USE_LLM` พร้อมกันทั้งหมด (ตามค่าจริงใน config นี้) หนึ่งคำถามของผู้ใช้จะทำให้เกิดการเรียกที่ "หนัก" ต่อกันเป็นทอด ๆ ตามลำดับใน `RAGPipeline.ask()` คือ (1) เรียก LLM ครั้งที่ 1 เพื่อสร้างคำถามหลายเวอร์ชัน (`multi_query`, `MULTI_QUERY_COUNT = 3`) (2) ค้นหาแบบ dense + BM25 ต่อคำถามแต่ละเวอร์ชัน (สูงสุด 4 เวอร์ชัน × 2 วิธีค้น) แล้วรวมด้วย RRF (3) ให้ cross-encoder reranker ประเมินคู่ query-document ทีละคู่จาก `CANDIDATE_K = 20` ตัวเลือก และ (4) เรียก LLM ครั้งที่ 2 เพื่อเขียนคำตอบสุดท้าย นี่คือเหตุผลเชิงโครงสร้างที่ทำไมการเปิดทุกฟีเจอร์พร้อมกันจึงมี **ต้นทุนด้าน latency สะสม** ที่ชัดเจนกว่าการเปิดทีละส่วน (ตรงกับที่ comment ในไฟล์ config ต้นฉบับเตือนไว้ว่า `USE_RERANK` "ช้ามาก" และ `USE_QUERY_TRANSFORM` "เสีย LLM เพิ่ม 1 ครั้งต่อคำถาม") การเลือกใช้ **Groq** เป็น provider (ซึ่งขึ้นชื่อเรื่อง inference เร็วกว่าผู้ให้บริการอื่นในราคาที่เข้าถึงได้) จึงเป็นตัวเลือกที่ช่วยลดผลกระทบของ LLM call 2 ครั้งต่อคำถามได้บางส่วน แต่ขั้นตอน rerank ที่รันบนเครื่องตัวเอง (ไม่ใช่ผ่าน API เร็ว ๆ แบบ LLM) ยังเป็นคอขวดหลักที่เห็นได้จริงเมื่อทดสอบใช้งาน

---

## 7. สรุปสำหรับเข้าห้องสอบ (โครงคำตอบเร็ว ๆ)

เวลาตอบข้อสอบ ให้ยึดโครงนี้ต่อ 1 ขั้นตอนของ pipeline: **(1) ปัญหาคืออะไร → (2) เกิดจากอะไร (root cause) → (3) รู้ได้อย่างไรว่าเกิดปัญหานี้ (การตรวจสอบ/metric) → (4) แก้ด้วยกลไกใดในโค้ด (อ้างอิงไฟล์/ฟังก์ชัน) → (5) ยังมีข้อจำกัดอะไรเหลืออยู่หลังแก้แล้ว** โครงนี้ใช้ได้กับทุกขั้นตอนที่อธิบายไว้ในเอกสารนี้ และการอ้างอิงชื่อไฟล์/ฟังก์ชัน/ตัวแปร config ที่ถูกต้อง (เช่น `index_meta.warn_if_stale()`, `reciprocal_rank_fusion()`, `NO_CONTEXT_MESSAGE`) จะทำให้คำตอบดูน่าเชื่อถือว่ามาจากการอ่านและเข้าใจ source code จริง ไม่ใช่คำตอบทั่วไปที่ท่องมา

จุดที่ควรเน้นเป็นพิเศษถ้าเวลาไม่พอ: **hallucination + faithfulness** (ข้อ 2.10) เพราะเป็นปัญหาเฉพาะของ RAG ที่ต่างจาก search engine ทั่วไป, **index staleness** (ข้อ 2.5) เพราะสะท้อนความเข้าใจเรื่อง data pipeline ทั้งระบบ, และที่สำคัญที่สุดคือ **ข้อ 6 (กรณีศึกษาจากระบบที่พัฒนาขึ้นจริงของตนเอง)** เพราะเป็นบั๊ก/ปัญหาที่ตรวจสอบได้จริงจากซอร์สโค้ดของระบบที่นักศึกษาพัฒนาต่อเอง (ไม่ใช่โค้ดของอาจารย์) ตอบโจทย์คำว่า "สอดคล้องกับระบบที่นักศึกษาพัฒนาขึ้นจริง" ได้ตรงที่สุด โดยเฉพาะข้อ 6.1 (`SHOW_SOURCES=True` ไม่มีผลเพราะโค้ดถูก comment ปิดไว้) ซึ่งอธิบายง่าย ตรวจสอบได้ทันทีที่รันโปรแกรม และมีหลักฐานในโค้ดสองไฟล์ (`config.py` กับ `main.py`) ยืนยันชัดเจน
